apiVersion: model.samzong.dev/v1
kind: ModelSource
metadata:
  name: sample-source
spec:
  type: HUGGING_FACE
  config:
    # Connection/auth configuration (no repo info here)
    endpoint: https://huggingface.co  # Optional: custom endpoint
    # token: <your-token>              # Optional: auth token (use Secret reference in production)
    # Common configuration (file filtering rules)
    include: "*.safetensors,*.json"   # Common: file patterns to include
    exclude: "*.bin"                  # Common: file patterns to exclude
