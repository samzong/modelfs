---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: models.model.samzong.dev
spec:
  group: model.samzong.dev
  names:
    kind: Model
    listKind: ModelList
    plural: models
    shortNames:
    - model
    singular: model
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.syncedVersions[*].name
      name: Versions
      type: integer
    - jsonPath: .spec.sourceRef
      name: Source
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Model describes a machine learning model instance tracked by
          the system.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ModelSpec contains desired attributes for a model.
            properties:
              display:
                description: Display contains display metadata for catalog purposes.
                properties:
                  description:
                    type: string
                  logoURL:
                    type: string
                  tags:
                    items:
                      type: string
                    type: array
                type: object
              sourceRef:
                description: SourceRef references a ModelSource in the same namespace.
                type: string
              versions:
                description: Versions defines all model versions and their configurations.
                items:
                  description: ModelVersion defines a specific version of a model.
                  properties:
                    metadata:
                      additionalProperties:
                        type: string
                      description: Metadata contains version-specific metadata (e.g.,
                        tags).
                      type: object
                    name:
                      description: Name is the unique version identifier (e.g., "fp16",
                        "q4", "v2.0.0").
                      type: string
                    precision:
                      description: Precision specifies the model precision.
                      enum:
                      - FP16
                      - INT4
                      - INT8
                      type: string
                    repo:
                      description: |-
                        Repo specifies the repository/path for this version. Format depends on the source type.
                        For HUGGING_FACE: format is "username/model-name" (e.g., "qwen/Qwen3-7B")
                        For S3: format is "bucket/path"
                        For GIT: format is the repository URL
                      type: string
                    revision:
                      description: 'Revision specifies the revision/branch/tag to
                        use (default: "main").'
                      type: string
                    share:
                      description: Share defines sharing configuration for this version.
                      properties:
                        enabled:
                          description: Enabled indicates whether sharing is enabled
                            for this version.
                          type: boolean
                        namespaceSelector:
                          description: NamespaceSelector selects namespaces that can
                            receive shared datasets.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        requireOptInLabel:
                          description: |-
                            RequireOptInLabel specifies a label key-value pair that namespaces must have to receive shares.
                            Format: "key=value" or just "key" (value defaults to "true").
                          type: string
                      required:
                      - enabled
                      type: object
                    state:
                      allOf:
                      - enum:
                        - PRESENT
                        - ABSENT
                      - enum:
                        - PRESENT
                        - ABSENT
                      default: PRESENT
                      description: 'State specifies whether this version should be
                        present or absent (default: PRESENT).'
                      type: string
                    storage:
                      description: Storage defines the PVC specification for this
                        version.
                      properties:
                        accessModes:
                          description: 'AccessModes defines the access modes for the
                            PVC (default: ReadWriteMany).'
                          items:
                            type: string
                          type: array
                        resources:
                          description: Resources defines the storage resource requirements.
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.

                                This field depends on the
                                DynamicResourceAllocation feature gate.

                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry in
                                  PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                  request:
                                    description: |-
                                      Request is the name chosen for a request in the referenced claim.
                                      If empty, everything from the claim is made available, otherwise
                                      only the result of this request.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        storageClassName:
                          description: StorageClassName specifies the storage class
                            to use.
                          type: string
                      required:
                      - resources
                      type: object
                  required:
                  - name
                  - repo
                  type: object
                minItems: 1
                type: array
            required:
            - sourceRef
            - versions
            type: object
          status:
            description: ModelStatus captures observed details about a model.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration tracks the generation of the Model
                  spec that was last reconciled.
                format: int64
                type: integer
              syncedVersions:
                description: SyncedVersions lists all versions and their observed
                  states.
                items:
                  description: SyncedVersion represents the observed state of a model
                    version.
                  properties:
                    activeDataset:
                      description: ActiveDataset is the name of the currently active
                        Dataset CR.
                      type: string
                    conditions:
                      description: Conditions contains conditions from the Dataset
                        status.
                      items:
                        description: Condition contains details for one aspect of
                          the current state of this API Resource.
                        properties:
                          lastTransitionTime:
                            description: |-
                              lastTransitionTime is the last time the condition transitioned from one status to another.
                              This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                            format: date-time
                            type: string
                          message:
                            description: |-
                              message is a human readable message indicating details about the transition.
                              This may be an empty string.
                            maxLength: 32768
                            type: string
                          observedGeneration:
                            description: |-
                              observedGeneration represents the .metadata.generation that the condition was set based upon.
                              For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                              with respect to the current state of the instance.
                            format: int64
                            minimum: 0
                            type: integer
                          reason:
                            description: |-
                              reason contains a programmatic identifier indicating the reason for the condition's last transition.
                              Producers of specific condition types may define expected values and meanings for this field,
                              and whether the values are considered a guaranteed API.
                              The value should be a CamelCase string.
                              This field may not be empty.
                            maxLength: 1024
                            minLength: 1
                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                            type: string
                          status:
                            description: status of the condition, one of True, False,
                              Unknown.
                            enum:
                            - "True"
                            - "False"
                            - Unknown
                            type: string
                          type:
                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                            maxLength: 316
                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                            type: string
                        required:
                        - lastTransitionTime
                        - message
                        - reason
                        - status
                        - type
                        type: object
                      type: array
                    lastSyncTime:
                      description: LastSyncTime is the last sync time from the Dataset
                        status.
                      format: date-time
                      type: string
                    name:
                      description: Name is the version name from spec.
                      type: string
                    observedState:
                      description: ObservedState is the observed state (PRESENT/ABSENT).
                      enum:
                      - PRESENT
                      - ABSENT
                      type: string
                    observedStorage:
                      anyOf:
                      - type: integer
                      - type: string
                      description: ObservedStorage is the observed storage capacity.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    observedVersionHash:
                      description: ObservedVersionHash is a hash of the version spec
                        for change detection.
                      type: string
                    phase:
                      description: Phase is the Dataset phase (Pending/Processing/Ready/Failed).
                      type: string
                    pvcName:
                      description: PVCName is the name of the PVC created for this
                        version.
                      type: string
                  required:
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
